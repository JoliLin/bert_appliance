# NLP tools

* segmentation
  * implement with BERT for Traditional Chinese by `CKIP`
  * requirements
    ```
    numpy
    torch  
    transformers 
    ```
* topic model
  * implement with BERT for Traditional Chinese by `CKIP`
  * requirements
    ```
    hdbscan
    numpy
    umap
    torch
    tqdm
    transformers
    sklearn
    ``` 
* tfidf  
  * requirements
    ``` 
    sklearn   
    ```
